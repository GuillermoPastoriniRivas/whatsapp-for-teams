export enum ConversationEventType {
  CREATED = 'created',
  ASSIGNED = 'assigned',
  REASSIGNED = 'reassigned',
  UNASSIGNED = 'unassigned',
  RESOLVED = 'resolved',
  REOPENED = 'reopened',
  NOTE_ADDED = 'note_added',
  HANDOFF = 'handoff',
}
