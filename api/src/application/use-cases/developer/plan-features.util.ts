/**
 * La resolución del plan vive en billing: es una sola regla para toda la app.
 * Este archivo queda como alias para no tocar los imports del módulo developer.
 */
export { resolvePlanFeatures } from '../billing/plan-resolution.util.js';
